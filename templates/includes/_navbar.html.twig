<nav class="navbar navbar-expand-lg bg-dark">
	<div class="container-fluid d-flex justify-content-between">
		<div class="d-flex align-items-center">
			<img src="{{ asset('build/images/logo.png') }}" alt="Wild Series logo">
			<a class="navbar-brand fs-2" href="#">Wild Series</a>
		</div>
		<button class="navbar-toggler btn-primary bg-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon bg-primary"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavDropdown">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_index') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('program_index') }}">Séries</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('category_index') }}">Catégories</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('actor_index') }}">Acteurs</a>
				</li>
			</ul>

			{# <form class="d-flex" role="search">
					<input id="input-dark" class="form-control me-2" type="search" placeholder="Recherche" aria-label="Search">
					<button class="btn btn-outline-success text-primary" type="submit">GO</button>
				</form> #}
		</div>
			<div class="justify-self-end">
				{% if app.user %}
				<span class="align-middle me-2">
					{{ app.user.email }}
				</span>
					<a class="btn btn-primary text-white" href="{{ path('app_logout') }}">Se déconnecter</a>
				{% else %}
					<a class="btn btn-primary text-white" href="{{ path('login') }}">Se connecter</a>
				{% endif %}
			</div>
	</div>
</nav>
